export function SiteFooter() {
  return (
    <footer className="site-footer">
      <div className="shell footer-row">
        <p>
          A clean App Router scaffold extracted from the structure of an existing production-style Next.js site, without copying its branding or content.
        </p>
        <nav className="nav">
          <a href="{{base_path}}/contact">Contact</a>
          <a href="{{base_path}}/privacy">Privacy</a>
        </nav>
      </div>
    </footer>
  );
}
