---
interface Props {
  siteName: string;
}

const { siteName } = Astro.props;
---

<footer class="site-footer">
  <div class="shell footer-row">
    <p>{siteName}</p>
    <p>Built from the clean `astro-site` template in `mofa-site`.</p>
  </div>
</footer>
